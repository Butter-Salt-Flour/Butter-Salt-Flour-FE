import React from 'react';
import { Button } from '@/components/Button';
const page = () => {
  return (
    <div>
      <Button variant="primary">gd</Button>
    </div>
  );
};

export default page;
